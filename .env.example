# Copy to .env.local. Do not commit secrets.
#
# Provision on the Vercel project, then pull:
#   npx vercel blob create-store intownconsultants-photos --access public --yes
#   npx vercel integration add neon
#   npx vercel env add ADMIN_PASSWORD
#   npx vercel env add AUTH_SECRET
#   npx vercel env pull .env.local --yes

ADMIN_PASSWORD=
AUTH_SECRET=
BLOB_READ_WRITE_TOKEN=
DATABASE_URL=
